export interface QueryAttributes {
    id: string;
    submissionDate: Date;
    customerId: string;
    employeeId: string;
    responseDate: Date;
    status: string;
    feedback: number;
    queryText: string;
    queryResponse: string;
}
